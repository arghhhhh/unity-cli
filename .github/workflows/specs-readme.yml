name: Specs README

on:
  pull_request:
    branches: [develop, main]

jobs:
  specs-readme:
    name: Verify specs/specs.md is up-to-date
    runs-on: ubuntu-latest
    timeout-minutes: 2

    steps:
      - name: Checkout code
        uses: actions/checkout@v6

      - name: Generate specs/specs.md
        run: bash .specify/scripts/bash/update-specs-readme.sh --quiet

      - name: Verify Speckit command invariants
        run: bash .specify/scripts/checks/check-speckit-commands.sh

      - name: Fail if specs/specs.md changed
        run: |
          status="$(git status --porcelain -- specs/specs.md)"
          if [[ -n "$status" ]]; then
            echo "ERROR: specs/specs.md が最新ではありません。"
            echo "ローカルで次を実行してコミットしてください:"
            echo "  bash .specify/scripts/bash/update-specs-readme.sh --quiet"
            echo ""
            echo "git status:"
            echo "$status"
            exit 1
          fi
