codecov:
  require_ci_to_pass: true

coverage:
  status:
    project:
      default:
        target: auto
    patch:
      default:
        target: auto

flags:
  rust:
    paths:
      - src/
  lsp:
    paths:
      - lsp/
